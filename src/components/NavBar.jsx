/**
 * El layout global (src/app/layout.js) ya renderiza el NavBar para todas las páginas.
 * Este componente existe por compatibilidad con páginas que lo importaban directamente.
 */
export default function NavBar() {
  return null;
}
