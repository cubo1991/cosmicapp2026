{
  "name": "cosmicapp-functions",
  "version": "2.0.0",
  "description": "Cloud Functions para CosmicApp: finalizacion de partidas y ciclo de copas",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0"
  }
}
